name: build-project
on: push
jobs:
  build:
    runs-on: macos-latest
    steps:
        - uses: actions/checkout@v3
        - uses: actions/setup-node@v3
        - run: make setup
        - name: run tests
          run: make test
        - name: run lint
          run: make lint

